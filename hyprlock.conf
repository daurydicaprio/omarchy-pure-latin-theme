# ==========================================================
#   Hyprlock Color Variables for Pure-Latin Theme
# ==========================================================

# Main background color
$color = rgba(38,42,51,1.0) # #262A33

# Inner elements, like the password field, with transparency
$inner_color = rgba(38,42,51,0.8)

# Outer elements, like the unlock ring, using our subtle accent
$outer_color = rgba(51,56,66,1.0) # #333842

# Font color for the clock and text
$font_color = rgba(211,222,233,1.0) # #D3DEE9

# Color for the "checking password" animation, using our main accent
$check_color = rgba(94,129,172,1.0) # #5E81AC
